os: linux
dist: focal
group: edge
keys:
  - DEV_TEST
  - NON_EXISTENT
script:
  - travis_key DEV_TEST cosign.key
  - cat cosign.key
  - chmod 600 cosign.key
  - ssh-keygen -y -e -f cosign.key
  - echo $TRAVIS_DEV_TEST | hd
